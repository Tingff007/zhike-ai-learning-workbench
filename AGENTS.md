# 项目 Agent 指令

## 全局行为规则

- 与用户沟通、任务说明、最终回复必须使用简体中文。
- 变量名、函数名、类名、库名、协议名、配置键、API 字段等遵循所在语言和生态的主流英文命名惯例，不为追求中文而破坏可读性或兼容性。
- 新增或修改代码时，所有说明性注释必须使用简体中文，详见后文“代码注释与文档字符串”章节。

## 固定开发端口

除非用户明确要求，否则使用项目既有端口：

- 前端 Vite 开发服务：`http://localhost:5173`
- 后端 FastAPI 服务：`http://localhost:8001`

如果端口已被占用，先检查是否已经是当前项目服务并复用。不要擅自启动 `5174`、`5175`、`5176` 等备用前端端口，也不要在未获用户明确同意时启动其他后端端口。

## 本地演示登录信息

- 本地测试或比赛演示需要管理员登录时，优先查看 `README.md` 中“管理员种子账号”章节。
- 不要在新增文档、代码注释、前端页面或测试日志中重复扩散明文密码；正式环境必须通过 `.env` 配置强密码。

## 前端适配范围

本项目仅面向 Web 浏览器体验。除非用户明确要求，不要主动适配、优化或重设计移动端竖屏、窄屏竖向布局。

## 前端 UI 布局规范

所有页面编写、页面重构、新增模块时必须严格遵循布局文档约定，禁止自行自定义排版样式。完整布局分类、结构示意、样式基线、使用场景与禁忌清单详见：`docs/layout-spec.md`

### 强制硬性规则

1. 页面顶级总标题统一使用 Page Header（裸页头、无容器、无卡片、无边框，主标题+底部说明文案结构）。
2. Page Header 的 Title 与 Subtitle 均使用简体中文，禁止附加英文标题或英文翻译。
3. Page Header 仅承载标题与说明文案，禁止塞入操作按钮、标签页、面包屑、筛选器等非标题元素。
4. Card Header 仅用于页面内区块小标题，且仅当区块需要容器包裹时才允许使用，不得替代 Page Header 承担页面顶级标题职责。
5. Page Header 与 Card Header 禁止混用、错用：页面顶部永远是 Page Header，页面内分区块才是 Card Header。
6. Page Header 的颜色、字号、间距必须引用 `frontend/src/styles/1-settings/variables.css` 中的设计令牌（如 `--settings-ink`、`--settings-muted`），禁止硬编码。
7. 修改布局规范时，先更新 `docs/layout-spec.md`，再落地代码。
8. 所有页面顶部 Page Header 必须遵循布局规范，基础标题样式全项目统一；标题下方的操作区只能使用 `PageHeaderToolbar` 容器并从「变体 1 左对齐操作栏 / 变体 2 左对齐 Tab 选项卡栏 / 变体 3 标题行右侧操作」3 种标准变体中选择，禁止自创排版结构。所有元素默认左对齐，禁止居中排布 Tab 与操作按钮，禁止把 Tab 与右侧操作按钮挤在同一 Toolbar 行导致 Tab 视觉居中，禁止把统计卡、Hero 容器、装饰背景等非标题元素与 Page Header 包裹在同一带边框/背景的容器中。完整规则见 `docs/layout-spec.md` 第 2.5 节。

### 开发要求

- 新增页面、重构页面或遇到布局边界不确定时，先查阅 `docs/layout-spec.md`；文档未覆盖的场景，先补充文档条款再落地代码。
- 触碰存量页面时，如发现相邻头部实现与本规范不一致，应在本任务内同步修正，不留形态不一致的局部上下文。

## 通用工程原则

- 通用工程需求优先复用项目已有能力和市场主流、活跃维护的成熟方案，避免重复实现已有可靠生态基础能力。涉及技术选型、依赖库、开源组件、文档处理、导入导出、AI 编排、前端组件、后端基础设施等通用能力时，优先采用市场主流、成熟、可维护的方案，避免手搓已有稳定生态方案的基础轮子。
- 自研代码应聚焦本产品的业务逻辑、流程编排、数据适配、权限规则和用户体验差异。
- 新增依赖要谨慎。引入前先确认现有技术栈是否足够，并评估成熟度、维护状态、文档质量、商业许可证、安全性、体积和运维成本。面向商业化或生产环境的实现必须同时考虑许可证、可替代性、安全、隐私、性能、可观测性、部署成本和后续维护成本。
- 区分原型验证和生产实现。临时代码必须边界清晰；生产相关实现需要考虑稳定性、测试、安全、可观测性、错误处理和可维护性。
- 保持清晰的架构和模块边界。新增能力放在合适层级，避免把无关职责混在一起。
- 默认重视安全与隐私，覆盖用户数据、生成内容、上传文件、密钥、模型调用和日志。使用最小必要权限、环境变量密钥和安全错误信息。
- 关键流程要可观测、可恢复。记录有意义的失败信息，暴露准确状态，并在可行时提供重试或降级路径。
- 用户可见行为、API、权限、数据结构或 AI 行为发生变化时，同步更新相关文档。

## 代码质量与架构

- 模块应高内聚、低耦合。每个模块只承担清晰职责，并暴露小而明确的接口。
- UI、API 路由、服务、仓储、模型、配置、AI 编排和基础设施代码之间保持清晰边界。
- 优先使用简单模块化设计，不做过早抽象。只有当抽象能减少真实重复或隔离关键复杂度时再引入。
- 通过类型、Schema、API 模型定义明确契约。
- 外部输入、AI 输出、上传文件、配置和数据库数据在使用前必须校验。
- 文件读写、网络请求、数据库写入、模型调用、缓存更新、后台任务等副作用应放在清晰的服务层或基础设施层。
- 错误处理要一致：提供有用日志、安全的用户提示，不泄露密钥、隐私数据或敏感内部细节。
- 重要业务逻辑应设计为可测试，并配套聚焦的单元测试或集成测试。
- 文件、函数、变量、类、组件和类型命名应清晰、符合领域语义，并遵循语言社区惯例。
- 配置与代码分离，避免硬编码环境相关值、路径、凭据、限制或功能开关。
- 修改应小步、连贯、可审查。避免把无关重构、格式化噪音和功能或修复混在一起。
- 修改 API、数据模型、权限、持久化存储或长期运行流程时，考虑向后兼容和迁移路径。

## AI 结构化输出与工具调用治理

新增或修改大模型调用、Agent 节点、结构化 JSON 输出、工具调用、资源生成、画像抽取、意图判别、Rubric 评分等能力时，必须遵循 `docs/04-ai-orchestration-design.md` 中“结构化输出与工具调用纵深防御”规范。

强制要求：

- 不能只依赖提示词保证格式正确；必须采用“提示词约束 + API 格式约束 + 后端代码校验 + 输出修复 / 重试 + 标准化降级”的链路。
- 模型供应商支持 `response_format`、JSON Schema、Function Calling、Tool Calling 或等价结构化输出能力时，必须优先启用；供应商不支持时，必须在 Adapter 或服务层显式记录能力缺口，并加强解析校验和失败降级。
- 后端必须使用 Pydantic、Zod、JSON Schema 或等价类型系统校验模型输出的语法、字段、类型、枚举、层级和业务规则；校验通过前不得写数据库、展示为可评分题单、触发工具副作用或进入审核通过状态。
- 工具调用必须由模型“提出工具名和参数”，由后端完成工具白名单、权限、参数 schema、幂等键、超时、限流和异常处理后再执行；禁止让模型直接决定或绕过后端执行真实副作用。
- 校验失败时应优先把具体错误回传给模型进行结构化修复；连续失败后返回稳定错误码和可重试状态，不得把不可解析、不可评分或未核验结果伪装成成功。
- 所有结构化输出失败、工具调用失败、修复重试和降级结果必须写入 trace 或调用日志，错误信息不得泄露密钥、隐私数据或敏感内部细节。

## 代码注释与文档字符串

- **注释语言总则**：新增或修改的说明性代码注释必须使用简体中文，覆盖单行注释、行内注释、块注释、Python docstring、JSDoc/TSDoc、TODO/FIXME、测试说明、配置说明、异常处理说明、迁移脚本说明和面向维护者的开发说明。禁止新增纯英文解释性注释。
- **英文保留范围**：第三方 API 字段、协议原文、错误码、标准术语、外部文档引用、变量名、函数名、类名、库名、协议名、配置键、API 字段可按主流惯例保留英文，但必要时应配套中文解释，避免维护者只能依赖英文上下文理解业务含义。
- **文档注释要求**：公共函数、公共类、导出组件、Hook、服务方法和复杂方法应编写中文 docstring，说明用途、参数、返回值、副作用、失败模式或异常情况。复杂算法、权限判断、安全策略、状态机、跨模块协作、AI Prompt/输出解析、协议适配、数据迁移、异常降级和兼容逻辑必须补充有维护价值的中文说明。
- **注释内容原则**：重点说明“为什么这样做”、业务规则、边界条件、不变量、风险和取舍，避免重复描述代码已经清楚表达的“做了什么”。不为简单直观代码堆砌注释；如果一段代码需要大量注释才能理解，应优先考虑通过命名、拆分函数、抽取模块或调整结构来提升可读性。
- **注释同步维护**：注释应跟随代码同步维护，修改逻辑时必须检查相邻注释、docstring 和 TODO 是否仍然准确。修改存量代码时，如触碰到邻近的英文注释、过期注释、误导注释或空泛注释，应在不改变语义的前提下同步修正为准确的中文说明，避免留下中英文混杂且难维护的局部上下文。不要求一次性全量翻译未触碰的历史英文注释。
- **TODO/FIXME 规范**：必须写清问题原因、期望后续动作和必要上下文，避免只写“临时处理”“以后优化”等无法指导维护的内容。
- **安全红线**：注释和文档字符串中不得包含密钥、令牌、隐私数据、内部凭据或敏感运维细节。

## 依赖镜像与编码

- 安装、更新或下载项目依赖时，除非用户明确要求，否则优先使用中国大陆镜像源。
- npm、pnpm、yarn 优先使用现有 `.npmrc` registry 配置；未经明确同意，不要替换为海外 registry。
- Python 依赖通过 `pip` 安装时优先使用清华、阿里云、USTC 等中国大陆 PyPI 镜像。
- 源码、Markdown、JSON、YAML、环境示例和配置文本文件统一视为 UTF-8。使用 PowerShell 或脚本读写时，如工具可能采用其他默认编码，必须显式指定 UTF-8。
- 不要引入 GBK/ANSI 编码文本文件。发现编码问题时，在保留内容的前提下转换为 UTF-8。

## 文档同步策略

当代码变更新增、删除、重命名或实质改变用户可见功能、路由、API、数据模型、权限、管理员流程、AI 编排行为、资源生成行为或验收标准时，必须在同一任务中同步更新相关文档。

必须遵循：

- 只更新与本次功能变更直接相关的文档，不重写无关章节；

常见文档目标：

- 产品范围或功能概览：`README.md`、`docs/01-product-overview.md`
- 路由、页面和导航变化：`docs/02-route-and-page-design.md`
- 页面布局、Page Header 或 Card Header 变化：`docs/layout-spec.md`
- 学习画像变化：`docs/03-learning-profile-design.md`
- AI 编排和 Agent 行为变化：`docs/04-ai-orchestration-design.md`
- 资源生成或审核变化：`docs/05-resource-generation-design.md`
- 管理后台变化：`docs/06-admin-design.md`
- API、数据模型、权限和验收变化：`docs/07-api-data-and-acceptance.md`
- 知识库、思维导图或技能设计变化：`docs/08-knowledge-mindmap-skill-design.md`

## 低层错误防护

在合并知识库或 WebSocket 相关工作前，必须检查：

- 如果辅助函数使用 `self.db` 或其他实例状态，绝不能标记为 `@staticmethod`。
- `ingestion-status` 阶段构建器位于 `backend/app/services/knowledge/ingestion_status_builder.py`；调用方必须使用 `meta_json=`，不要使用 `meta=`。
- WebSocket 鉴权只能在 `CONNECTING` 状态下执行 `accept()`（`app/core/ws_auth.py` + `app/core/ws_utils.py`）。
- 导入进度 UI 在轮询失败时不能显示虚假的固定百分比；应使用 `frontend/src/utils/knowledgeIngestion.ts`。
