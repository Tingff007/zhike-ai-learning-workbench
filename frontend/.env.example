# 后端 API 根路径（开发默认代理到 http://localhost:8001/api/v1）
VITE_API_BASE_URL=/api/v1

# Mock 数据总开关（唯一数据源开关）
# true  → 全站 API 层返回 frontend/src/mocks/fixtures 按业务模块组织的演示数据
# false → 全站请求真实后端（默认）
VITE_USE_MOCKS=false

# 可选：知识库上传超时（毫秒）
# VITE_KNOWLEDGE_UPLOAD_TIMEOUT_MS=180000
