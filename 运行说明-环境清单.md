# 智课未来运行说明与环境清单

## 一、环境清单

运行本项目需要提前安装：

- Node.js 20 或更高版本，建议使用随 Node 自带的 npm。
- Python 3.12.x，后端要求 `>=3.12,<3.13`。
- Docker Desktop，推荐用于启动 PostgreSQL、pgvector 和 Valkey/Redis。
- Git 可选，仅用于代码管理。

如不使用 Docker，需要自行安装：

- PostgreSQL 16，并启用 pgvector 扩展。
- Redis 或 Valkey。

## 二、首次配置

在项目根目录复制环境变量模板：

```powershell
copy .env.example .env
```

正式环境请修改 `.env` 中的密钥和密码。本文件包内不包含 `.env`，避免泄露本机配置。

## 三、启动前端

```powershell
cd frontend
npm.cmd ci --registry=https://registry.npmmirror.com --no-audit --no-fund
npm.cmd run dev -- --host 127.0.0.1 --port 5173
```

前端地址：

```text
http://127.0.0.1:5173
```

只看前端演示效果可打开：

```text
http://127.0.0.1:5173/login?mock=1
```

## 四、启动后端

另开一个 PowerShell，在项目根目录执行：

```powershell
cd backend
py -3.12 -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
python run_dev.py
```

后端地址：

```text
http://127.0.0.1:8001
```

API 文档：

```text
http://127.0.0.1:8001/docs
```

## 五、基础设施

推荐用 Docker 启动数据库和缓存：

```powershell
docker compose up -d postgres valkey
```

端口约定：

- 前端：`5173`
- 后端：`8001`
- PostgreSQL：`5432`
- Valkey/Redis：`6379`
- 代码沙箱：`8003`

## 六、注意事项

- 不要把 `node_modules`、`.npm-cache`、`dist`、`.venv`、`.env` 放进交付包。
- 如果 npm 安装很慢，看到 `GET 200` 和 `cache miss` 不是失败，只是首次下载依赖。
- 真实登录需要后端启动；只看界面演示可以用 `?mock=1`。
